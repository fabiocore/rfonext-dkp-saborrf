DATABASE_URL="postgresql://rfonext:rfonext_dev_password@localhost:5432/rfonext_dkp?schema=public"

PORT=3000
JWT_SECRET="change-me-in-production-please"
JWT_EXPIRES_IN="12h"

# Bootstrap do GM — usado só na primeira inicialização se ainda não existir um GM no banco.
GM_BOOTSTRAP_USERNAME="guildmaster"
GM_BOOTSTRAP_PASSWORD="change-me-on-first-login"

UPLOADS_DIR="./uploads"
